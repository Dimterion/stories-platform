export default function Instructions() {
  return (
    <section className="space-y-2">
      <h3 className="text-center text-xl font-bold">Instructions</h3>
      <p>
        Use Story Player to read/play through the interactive stories (upload
        your own stories in JSON format).
      </p>
      <p>
        Use Story Editor to create interactive stories (upload your own stories
        in JSON format, or download created stories in HTML or JSON format).
      </p>
      <p>
        Review your stories structure in Story Editor by using Story diagram
        functionality (download your stories diagrams in SVG or PNG format).
      </p>
    </section>
  );
}
